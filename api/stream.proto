syntax = "proto3";

package stream;
option go_package = "github.com/jmesmon/wgrc/api";

service Streamer {
	rpc Listen(ListenReq) returns (stream ListenEvent);
}

message ListenReq {
	string hi = 1;
}

message ListenEvent {
	string other = 1;
	uint64 val = 2;
}
